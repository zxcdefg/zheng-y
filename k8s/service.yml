apiVersion: v1
kind: Service
metadata:
  name: nhdy
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "nhdy"
    port: 8080
  selector:
    app: nhdy
